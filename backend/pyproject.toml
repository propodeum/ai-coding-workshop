[project]
name = "backend"
version = "0.1.0"
description = "Workshop backend"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "openai>=1.54",
    "python-dotenv>=1.0",
]
